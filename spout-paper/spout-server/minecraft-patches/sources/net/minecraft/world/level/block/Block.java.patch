--- a/net/minecraft/world/level/block/Block.java
+++ b/net/minecraft/world/level/block/Block.java
@@ -75,11 +_,14 @@
 import org.jspecify.annotations.Nullable;
 import org.slf4j.Logger;
 
-public class Block extends BlockBehaviour implements ItemLike {
+// Spout start - More data-driven - Minecraft registries - Block - Implement interfaces
+public class Block extends BlockBehaviour implements ItemLike, spout.gamecontent.datadriven.block.BlockCodecAccessor, spout.gamecontent.datadriven.block.BlockTypeDecorator {
+    @Override public MapCodec<? extends Block> spout$codec() { return this.codec(); }
+    // Spout end - More data-driven - Minecraft registries - Block - Implement interfaces
     public static final MapCodec<Block> CODEC = simpleCodec(Block::new);
     private static final Logger LOGGER = LogUtils.getLogger();
     private final Holder.Reference<Block> builtInRegistryHolder = BuiltInRegistries.BLOCK.createIntrusiveHolder(this);
-    public static final IdMapper<BlockState> BLOCK_STATE_REGISTRY = new IdMapper<>();
+    public static final spout.server.paper.impl.moredatadriven.minecraft.BlockStateRegistry BLOCK_STATE_REGISTRY = spout.server.paper.impl.moredatadriven.minecraft.BlockStateRegistry.get(); // Spout - More data-driven - Minecraft registries - Custom registries for blocks and items
     private static final LoadingCache<VoxelShape, Boolean> SHAPE_FULL_BLOCK_CACHE = CacheBuilder.newBuilder()
         .maximumSize(512L)
         .weakKeys()
@@ -145,6 +_,73 @@
     protected MapCodec<? extends Block> codec() {
         return CODEC;
     }
+
+    public @Nullable List<spout.server.paper.impl.packetmapping.block.datadriven.UnappliedDataDrivenBlockMapping> unappliedDataPackMappings = null; // Spout - Block mapping - Data-driven - Load mappings from data pack
+
+    // Spout start - More data-driven - Minecraft registries - Cache block and item values
+    /**
+     * The {@link net.minecraft.resources.Identifier} with which this block is registered in the
+     * {@link BuiltInRegistries#BLOCK}.
+     *
+     * <p>
+     * This will be null until initialized in
+     * {@link BlockRegistry#register}.
+     * </p>
+     */
+    public net.minecraft.resources.Identifier keyInBlockRegistry = null;
+
+    /**
+     * The {@linkplain net.minecraft.core.Registry#getId index} at which this block is registered in
+     * {@link BuiltInRegistries#BLOCK}.
+     *
+     * <p>
+     * This will be -1 until initialized in
+     * {@link BlockRegistry#register}.
+     * </p>
+     */
+    public int indexInBlockRegistry = -1;
+
+    /**
+     * The {@linkplain net.minecraft.core.Registry#getId index} at which this block is registered in
+     * {@link VanillaOnlyBlockRegistry}.
+     *
+     * <p>
+     * This will be -1 until initialized in
+     * {@link VanillaOnlyBlockRegistry#add}.
+     * </p>
+     */
+    public int indexInVanillaOnlyBlockRegistry = -1;
+    // Spout end - More data-driven - Minecraft registries - Cache block and item values
+
+    // Spout start - More data-driven - Vanilla flag - Implementation part
+    private boolean isVanilla;
+
+    /**
+     * Returns whether this block is present in vanilla.
+     *
+     * <p>
+     * This does not take into account whether any details of this block, such as its hardness,
+     * are different (were modified) from vanilla.
+     * </p>
+     *
+     * @return Whether this block is present in vanilla.
+     */
+    public boolean isVanilla() {
+        return this.isVanilla;
+    }
+
+    /**
+     * Sets {@link #isVanilla} to true.
+     *
+     * <p>
+     * This must only be called at most once,
+     * before the block is registered to {@link BuiltInRegistries#BLOCK}.
+     * </p>
+     */
+    public void markAsVanilla() {
+        this.isVanilla = true;
+    }
+    // Spout end - More data-driven - Vanilla flag - Implementation part
 
     public static int getId(final @Nullable BlockState blockState) {
         if (blockState == null) {
